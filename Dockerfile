FROM toposoid/toposoid-scala-lib:0.6WORKDIR /appARG TARGET_BRANCHENV DEPLOYMENT=localRUN apt-get update \&& apt-get -y install git \&& git clone https://github.com/toposoid/toposoid-knowledge-register-subscriber.git \&& cd toposoid-knowledge-register-subscriber \&& git pull \&& git fetch origin ${TARGET_BRANCH} \&& git checkout ${TARGET_BRANCH} \&& sbt publishLocal \&& rm -Rf ./targetCOPY ./docker-entrypoint.sh /app/ENTRYPOINT ["/app/docker-entrypoint.sh"]